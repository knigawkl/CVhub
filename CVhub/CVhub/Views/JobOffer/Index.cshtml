@model List<JobOffer>

@{
    ViewData["Title"] = "Job Offer list";
}

<h2>Job Offer list</h2>
<ul>
    @foreach (var offer in Model.OrderBy(x => x.JobTitle))
    {
        <li>@Html.ActionLink(offer.JobTitle, "Details", new { id = offer.Id })</li>
    }
</ul>